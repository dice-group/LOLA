Follow the steps below to start training the LORAs on a sample custom data.

### Setup
Run the following to setup the python virtual environment (tested with python 3.12):
```bash
bash setup_peft_venv.sh
```

### Train
Once the setup is complete, you can start training the model using:
```bash
bash train_lola_peft.sh
```